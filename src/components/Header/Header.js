import "./Header.scss";

const Header = () => {
    // Need a user prop to render username dynamically
  return (
    <header className="header">
      <h1 className="header__title">WELCOME BACK,"Daisy12"</h1>
    </header>
  );
};

export default Header;